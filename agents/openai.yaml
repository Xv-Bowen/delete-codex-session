interface:
  display_name: "Delete Codex Session"
  short_description: "Delete sessions using ownership and effect evidence"
  default_prompt: "Use $delete-codex-session to judge authoritative ownership and shadow mutation effects, report the exact scope, obtain one combined scope-and-deletion approval, and use one native-AppleScript Ghostty window for offline work."
policy:
  allow_implicit_invocation: true
